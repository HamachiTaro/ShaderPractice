#pragma kernel CSMain

RWTexture2D<float4> ResultBuffer;

float2 ScreenSize;

[numthreads(8,8,1)]
void CSMain (uint3 id : SV_DispatchThreadID)
{
    ResultBuffer[id.xy] = float4(id.x / ScreenSize.x, id.y / ScreenSize.y, 0.0, 1.0); 
}
